pipeline {
   agent any
    
   tools{
        
         dockerTool 'docker'
    }

   stages{
   
           stage("Checkout code") {
            steps {
                checkout scm
            }
        }   
      
   }   
}
