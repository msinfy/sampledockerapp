pipeline {
   agent any
    
   tools{
        
         dockerTool 'docker'
    }

   stages{
    stage('Clone repository') {
        /* Cloning the Repository to our Workspace */

        checkout scm
       }
      
      
      
   }   
}
